# receipt-ocr
This project builds an end-to-end AI system that converts receipt images into structured digital data and exposes the result through REST API, allowing client applications to upload receipt images and receive structured JSON outputs of the receipt
